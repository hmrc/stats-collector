# Copyright 2023 HM Revenue & Customs
#

include "backend.conf"

# This is the main configuration file for the application.
# ~~~~~

appName = stats-collector

play.modules.enabled += "uk.gov.hmrc.play.bootstrap.HttpClientV2Module"
play.modules.enabled += "uk.gov.hmrc.stats.StatsCollectorModule"

#########################

# Define any modules used here
play.modules.enabled += "uk.gov.hmrc.mongo.play.PlayMongoModule"


# Session Timeout
# ~~~~
# The default session timeout for the app is 15 minutes (900seconds).
# Updating this is the responsibility of the app - it must issue a new cookie with each request or the session will
# timeout 15 minutes after login (regardless of user activity).
# session.maxAge=900

# Session configuration
# ~~~~~
application.session.httpOnly = false

application.session.secure = false

# The application languages
# ~~~~~
application.langs = "en"

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routes`.
# Default to Routes in the root package (and conf/routes)
# !!!WARNING!!! DO NOT CHANGE THIS ROUTER
play.http.router = prod.Routes


# Controller
# ~~~~~
# By default all controllers will have authorisation, logging and 
# auditing (transaction monitoring) enabled.
# The below controllers are the default exceptions to this rule.

controllers {
  com.kenshoo.play.metrics.MetricsController = {
    needsLogging = false
    needsAuditing = false
  }
}

# increase timeout to be able to process slow responses from preferences
ws.timeout.request=60000

txmonitoring {
  enabled = true
  traceRequests = true
}

mongodb {
  uri = "mongodb://localhost:27017/stats-collector"
  channels = 5
  failoverStrategy = {
    initialDelayMsecs = 100
    retries = 10
    delay = {
      function = fibonacci
      factor = 1
    }
  }
}

microservice {
  services {
    timeout = 5 seconds

    delay-response = 2 seconds

    protocol = http

    datastream {
      host = localhost
      port = 8100
    }

    preferences {
      host = localhost
      port = 8025
    }
  }

  stats-generator {
    schedule = "02:30",
    jobs = {
      print-suppression = {
        audit-type = "yta-mis"
        service = preferences
        api = "/preferences/stats"
      }
    }
  }

  stats-collector {
    schedule = "03:00",
    jobs = {
       print-suppression = {
        audit-type = "yta-mis"
        service = preferences
        api = "/preferences/stats"
      }
    }
  }
}
